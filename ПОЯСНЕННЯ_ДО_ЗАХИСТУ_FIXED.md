# 📚 Пояснення до лабораторних робіт - Підготовка до захисту

## 🎯 Загальна структура роботи

Ваші лабораторні роботи охоплюють основні теми програмування на JavaScript:
- **LB1**: Перша веб-сторінка (HTML, CSS, JS)
- **LB2**: Обчислення та логічні операції
- **LB3**: Робота з масивами (одномірні та двомірні)
- **LB4**: Алгоритми сортування (Heap Sort, Bubble Sort)
- **LB5**: Візуалізація даних за допомогою Chart.js

---

## 🌐 ЛАБОРАТОРНА РОБОТА №1 - Перша веб-сторінка

### **Що ви робите:**
Створюєте базову HTML-сторінку з підключенням CSS стилів та JavaScript.

### **Структура HTML:**

```html
<!DOCTYPE html>
<html>
<head>
    <title>My First Lab Website</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>Hello, World!</h1>
    <h2>Денисенко Богдан</h2>
    <h3>Course: Разом зі спеціальністю, Group: УБД-21</h3>
    <script src="script.js"></script>
</body>
</html>
```

**Пояснення елементів:**

#### 1️⃣ **DOCTYPE та структура**
```html
<!DOCTYPE html>
```
- Вказує браузеру, що це HTML5 документ
- Обов'язкова декларація на початку файлу

#### 2️⃣ **Head секція**
```html
<head>
    <title>My First Lab Website</title>
    <link rel="stylesheet" href="style.css">
</head>
```
- `<title>` - назва вкладки браузера
- `<link>` - підключення зовнішнього CSS файлу
- `rel="stylesheet"` - вказує тип зв'язку

#### 3️⃣ **Body секція**
```html
<h1>Hello, World!</h1>
<h2>Денисенко Богдан</h2>
<h3>Course: Разом зі спеціальністю, Group: УБД-21</h3>
```
- `<h1>` - заголовок 1-го рівня (найбільший)
- `<h2>` - заголовок 2-го рівня
- `<h3>` - заголовок 3-го рівня
- Ієрархія: h1 > h2 > h3 > h4 > h5 > h6

#### 4️⃣ **Підключення JavaScript**
```html
<script src="script.js"></script>
```
- Розміщується перед закриваючим `</body>`
- Завантажується після HTML (сторінка вже готова)

### **JavaScript файл:**

```javascript
console.log("Welcome to the First Lab Website!");
```

**Що робить:**
- `console.log()` - виводить повідомлення в консоль браузера (F12)
- Використовується для налагодження (debugging)

---

## 📝 ЛАБОРАТОРНА РОБОТА №2 - Обчислення та логічні операції

### **Що ви робите:**
Реалізуєте різноманітні математичні обчислення та логічні перевірки, працюєте з координатами, датами та числами.

### **Основні завдання та методи:**

---

#### 1️⃣ **Завдання 1: Периметр і площа трикутника**

```javascript
let a = Math.sqrt((x2 - x1) ** 2 + (y2 - y1) ** 2);
let b = Math.sqrt((x3 - x2) ** 2 + (y3 - y2) ** 2);
let c = Math.sqrt((x1 - x3) ** 2 + (y1 - y3) ** 2);

let P = a + b + c;
let S = Math.abs((x1 * (y2 - y3) + x2 * (y3 - y1) + x3 * (y1 - y2)) / 2);
```

**Формули:**
- **Відстань між точками:** `d = √((x2-x1)² + (y2-y1)²)` - формула Піфагора
- **Периметр:** `P = a + b + c` - сума всіх сторін
- **Площа (формула Герона):** Використовується векторний добуток

**Пояснення для викладача:**
- `Math.sqrt()` - квадратний корінь
- `** 2` - піднесення до квадрату (еквівалент `Math.pow(x, 2)`)
- `Math.abs()` - модуль числа (завжди додатнє значення)
- `.toFixed(2)` - округлення до 2 знаків після коми

---

#### 2️⃣ **Завдання 2: Координати вершини параболи**

```javascript
let xv = -b / (2 * a);
let yv = a * xv * xv + b * xv + c;
```

**Математика:**
- Парабола має вигляд: `y = ax² + bx + c`
- Координата X вершини: `xv = -b / (2a)`
- Координата Y: підставляємо `xv` в рівняння параболи

**Валідація:**
```javascript
if (a == 0) {
    document.getElementById('res2').innerText = 'Помилка: a не може бути 0';
    return;
}
```
**Чому важливо:** Якщо `a = 0`, це не парабола, а пряма лінія!

---

#### 3️⃣ **Завдання 3: Підрахунок від'ємних чисел**

```javascript
let negativeCount = 0;
if (a < 0) negativeCount++;
if (b < 0) negativeCount++;
if (c < 0) negativeCount++;
```

**Логіка:**
- Перевіряємо кожне число окремо
- Інкрементуємо лічильник (`++`) при виконанні умови
- Просто і зрозуміло

**Альтернативний спосіб (більш професійний):**
```javascript
let negativeCount = [a, b, c].filter(x => x < 0).length;
```

---

#### 4️⃣ **Завдання 4: Перевірка ділення на 5**

```javascript
if (number % 5 == 0) {
    // Ділиться на 5
} else {
    // Не ділиться
}
```

**Оператор модуля (`%`):**
- Повертає остачу від ділення
- `10 % 5 = 0` (ділиться без остачі)
- `12 % 5 = 2` (остача 2)

---

#### 5️⃣ **Завдання 5: Перевірка нерівності b > a > c**

```javascript
let result = (b > a) && (a > c);
```

**Логічні оператори:**
- `&&` (AND) - "І", обидві умови мають бути істинними
- `>` - більше
- Результат: `true` або `false`

**Порядок виконання:**
1. Перевіряємо `b > a`
2. Перевіряємо `a > c`
3. Обидві умови мають бути `true`

---

#### 6️⃣ **Завдання 6: Щасливе число**

```javascript
let str = num.toString();
let sum1 = Number(str[0]) + Number(str[1]) + Number(str[2]);
let sum2 = Number(str[3]) + Number(str[4]) + Number(str[5]);

if (sum1 == sum2) {
    // Щасливе!
}
```

**Робота з рядками:**
- `toString()` - перетворює число в рядок
- `str[0]` - доступ до окремого символу (як до масиву)
- `Number()` - перетворює символ назад в число

**Приклад:**
- Число: `123321`
- `sum1 = 1 + 2 + 3 = 6`
- `sum2 = 3 + 2 + 1 = 6`
- `6 == 6` → щасливе!

**Валідація:**
```javascript
if (num < 100000 || num > 999999) {
    // Не шестизначне
}
```

---

#### 7️⃣ **Завдання 7: День тижня**

```javascript
let dayIndex = (k + 5) % 7;

switch(dayIndex) {
    case 0: dayName = 'Неділя'; break;
    case 1: dayName = 'Понеділок'; break;
    // ...
}
```

**Пояснення обчислення:**
- 1 січня - субота (індекс 6)
- `k = 1` → `(1 + 5) % 7 = 6` → Субота ✓
- `k = 2` → `(2 + 5) % 7 = 0` → Неділя ✓
- Формула: зсув на 5 днів + модуль 7 (тиждень)

**Оператор `switch`:**
- Вибір з багатьох варіантів
- `break` - вихід з switch (без нього виконаються всі наступні case!)
- Зручніше за багато `if-else`

---

#### 8️⃣ **Завдання 8: Попередня дата**

```javascript
if (n > 1) {
    prevDay = n - 1;
    prevMonth = t;
} else {
    prevMonth = (t == 1) ? 12 : t - 1;
    // Визначаємо останній день попереднього місяця
}
```

**Логіка:**
- Якщо не 1-ше число → просто віднімаємо 1
- Якщо 1-ше число → переходимо на попередній місяць
- Якщо січень → переходимо на грудень

**Тернарний оператор:**
```javascript
prevMonth = (t == 1) ? 12 : t - 1;
```
Еквівалент:
```javascript
if (t == 1) {
    prevMonth = 12;
} else {
    prevMonth = t - 1;
}
```

**Визначення днів у місяці:**
```javascript
switch(t) {
    case 1: case 3: case 5: case 7: case 8: case 10: case 12:
        daysInMonth = 31; break;
    case 4: case 6: case 9: case 11:
        daysInMonth = 30; break;
    case 2:
        daysInMonth = 28; break;
}
```

---

### **Основні концепції LB2:**

#### ✅ **Отримання значень з форм**
```javascript
let x1 = parseFloat(document.getElementById('x1').value);
```
- `document.getElementById()` - знаходимо елемент
- `.value` - отримуємо введене значення
- `parseFloat()` - перетворюємо рядок в число з комою
- `parseInt()` - перетворюємо в ціле число

#### ✅ **Валідація введених даних**
```javascript
if (isNaN(x1) || isNaN(y1)) {
    alert('Помилка: Введіть коректні дані');
    return;
}
```
- `isNaN()` - перевірка "чи це не число" (Not a Number)
- `return` - виходимо з функції, не виконуючи далі

#### ✅ **Виведення результатів**
```javascript
document.getElementById('res1').innerText = `Периметр: ${P.toFixed(2)}`;
```
- **Template literals** (шаблонні рядки): `` `текст ${змінна}` ``
- `${P}` - вставка значення змінної в рядок
- `.innerText` - встановлює текстовий вміст елемента

---

## 📊 ЛАБОРАТОРНА РОБОТА №3 - Масиви

### **Що ви робите:**
Працюєте з одномірними та двомірними масивами, генеруєте псевдовипадкові числа та виконуєте математичні операції над ними.

### **Основні методи та концепції:**

#### 1️⃣ **Генерація випадкових чисел**
```javascript
Math.floor(Math.random() * (max - min + 1)) + min
```
**Пояснення для викладача:**
- `Math.random()` генерує випадкове число від 0 до 0.999...
- Множимо на діапазон `(max - min + 1)` щоб отримати потрібний діапазон
- `Math.floor()` округлює вниз до цілого числа
- Додаємо `min` щоб зсунути діапазон до потрібних меж

**Приклад:** Для діапазону 100 до 500:
- `Math.random() * 401` дає число від 0 до 400.999
- `Math.floor(...)` робить його цілим (0 до 400)
- Додавання `100` зміщує діапазон до 100...500

---

#### 2️⃣ **Робота з одномірними масивами**

**Функція `generateRandomArray(size, min, max)`**
```javascript
function generateRandomArray(size, min, max) {
    const array = [];
    for (let i = 0; i < size; i++) {
        array.push(Math.floor(Math.random() * (max - min + 1)) + min);
    }
    return array;
}
```

**Що робить:** Створює масив заданого розміру з випадковими числами в заданому діапазоні

**Метод `push()`:** Додає новий елемент в кінець масиву

**Основні концепції:**
- Використання циклу `for` для ітерації
- Метод `push()` для додавання елементів в масив
- Повернення результату через `return`

---

#### 3️⃣ **Робота з двомірними масивами**

**Функція `generateRandom2DArray(rows, cols, min, max)`**
```javascript
function generateRandom2DArray(rows, cols, min, max) {
    const array = [];
    for (let i = 0; i < rows; i++) {
        const row = [];
        for (let j = 0; j < cols; j++) {
            row.push(Math.floor(Math.random() * (max - min + 1)) + min);
        }
        array.push(row);
    }
    return array;
}
```

**Що робить:** Створює двомірний масив (матрицю)

**Пояснення:**
- Зовнішній цикл створює рядки
- Внутрішній цикл заповнює кожен рядок стовпцями
- Двомірний масив це "масив масивів"
- Доступ до елементів: `array[рядок][стовпець]`

---

#### 4️⃣ **Пошук мінімального парного елемента**

```javascript
let minEven = null;
for (let i = 0; i < array.length; i++) {
    if (array[i] % 2 === 0) {  // Перевірка на парність
        if (minEven === null || array[i] < minEven) {
            minEven = array[i];
        }
    }
}
```

**Алгоритм:**
1. Ініціалізуємо змінну `minEven = null`
2. Перебираємо всі елементи масиву
3. Перевіряємо парність через `% 2 === 0` (остача від ділення на 2)
4. Якщо елемент парний І менший за поточний мінімум - оновлюємо
5. Якщо парних немає - повертаємо перший елемент

---

#### 5️⃣ **Пошук мінімуму та максимуму**

```javascript
let minValue = array[0];
let maxValue = array[0];

for (let i = 1; i < array.length; i++) {
    if (array[i] < minValue) minValue = array[i];
    if (array[i] > maxValue) maxValue = array[i];
}
```

**Чому ефективно:** Один прохід по масиву (складність O(n))

---

#### 6️⃣ **Підрахунок позитивних елементів в 2D масиві**

```javascript
let count = 0;
for (let i = 0; i < array.length; i++) {
    for (let j = 0; j < array[0].length; j++) {
        if (array[i][j] > 0) {
            count++;
        }
    }
}
```

**Вкладені цикли:** Для обходу всіх елементів двомірного масиву

---

## 🔄 ЛАБОРАТОРНА РОБОТА №4 - Алгоритми сортування

### **Завдання 1.1: Heap Sort (Пірамідальне сортування)**

#### **Що таке Heap Sort?**
Heap Sort - це алгоритм сортування, який використовує структуру даних "купа" (heap). Він працює в два етапи:
1. Побудова купи з масиву
2. Послідовне вилучення максимального елемента

#### **Основні методи:**

##### 1️⃣ **Функція `heapSort(arr)`**
```javascript
function heapSort(arr) {
    let n = arr.length;
    
    // Побудова купи (перегрупування масиву)
    for (let i = Math.floor(n / 2) - 1; i >= 0; i--) {
        heapify(arr, n, i);
    }
    
    // Вилучення елементів з купи один за одним
    for (let i = n - 1; i > 0; i--) {
        // Переміщуємо поточний корінь в кінець
        let temp = arr[0];
        arr[0] = arr[i];
        arr[i] = temp;
        
        // Викликаємо heapify на зменшеній купі
        heapify(arr, i, 0);
    }
    
    reverseArray(arr);
}
```

**Пояснення для викладача:**
- **Крок 1:** Будуємо max-heap (максимальна купа) - дерево де батько >= дітей
- **Крок 2:** Забираємо корінь (максимум), міняємо місцями з останнім елементом
- **Крок 3:** Відновлюємо властивості купи для зменшеного масиву
- **Крок 4:** Повторюємо до повного сортування
- **Крок 5:** Реверсуємо для отримання порядку за спаданням

**Складність:** O(n log n) - ефективний алгоритм!

---

##### 2️⃣ **Функція `heapify(arr, n, i)`**
```javascript
function heapify(arr, n, i) {
    let largest = i;        // Ініціалізуємо найбільший як корінь
    let left = 2 * i + 1;   // лівий дочірній = 2*i + 1
    let right = 2 * i + 2;  // правий дочірній = 2*i + 2
    
    // Якщо лівий дочірній більший за корінь
    if (left < n && arr[left] > arr[largest]) {
        largest = left;
    }
    
    // Якщо правий дочірній більший за найбільший
    if (right < n && arr[right] > arr[largest]) {
        largest = right;
    }
    
    // Якщо найбільший не корінь
    if (largest !== i) {
        let temp = arr[i];
        arr[i] = arr[largest];
        arr[largest] = temp;
        
        // Рекурсивно heapify піддерево
        heapify(arr, n, largest);
    }
}
```

**Що робить heapify:**
- Перетворює піддерево з коренем `i` в купу
- Порівнює батьківський елемент з дочірніми
- Якщо дочірній більший - міняє місцями
- Рекурсивно продовжує вниз по дереву

**Формули індексів в бінарній купі:**
- Лівий дочірній: `2 * i + 1`
- Правий дочірній: `2 * i + 2`
- Батьківський: `Math.floor((i - 1) / 2)`

---

##### 3️⃣ **Робота з двомірним масивом**
```javascript
// Розгортаємо 2D масив в 1D
let compose = [];
for (let i = 0; i < array.length; i++) {
    for (let j = 0; j < array[i].length; j++) {
        compose[compose.length] = array[i][j];
    }
}

// Сортуємо
heapSort(compose);

// Згортаємо назад в 2D
let index = 0;
for (let i = 0; i < array.length; i++) {
    for (let j = 0; j < array[i].length; j++) {
        array[i][j] = compose[index];
        index++;
    }
}
```

**Чому так робимо:** Heap Sort працює з одномірними масивами, тому спочатку перетворюємо 2D → 1D, сортуємо, потім повертаємо в 2D формат.

---

### **Завдання 1.2: Bubble Sort за індексами**

#### **Що таке Bubble Sort?**
Bubble Sort (бульбашкове сортування) - найпростіший алгоритм сортування. Великі елементи "спливають" на свої позиції як бульбашки.

##### 1️⃣ **Сортування за зростанням**
```javascript
function bubbleSortAscending(arr) {
    let n = arr.length;
    let swapped;
    
    for (let i = 0; i < n - 1; i++) {
        swapped = false;
        for (let j = 0; j < n - 1 - i; j++) {
            if (arr[j] > arr[j + 1]) {
                // Обмін елементів
                let temp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = temp;
                swapped = true;
            }
        }
        if (!swapped) break;  // Оптимізація: якщо не було обмінів - масив вже відсортований
    }
}
```

**Як працює:**
1. Порівнюємо сусідні елементи
2. Якщо лівий > правого - міняємо місцями
3. Проходимо весь масив
4. Повторюємо поки є обміни

**Складність:** O(n²) - повільний для великих масивів

---

##### 2️⃣ **Сортування за спаданням**
```javascript
function bubbleSortDescending(arr) {
    let n = arr.length;
    let swapped;
    
    for (let i = 0; i < n - 1; i++) {
        swapped = false;
        for (let j = 0; j < n - 1 - i; j++) {
            if (arr[j] < arr[j + 1]) {  // Змінили знак порівняння!
                let temp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = temp;
                swapped = true;
            }
        }
        if (!swapped) break;
    }
}
```

**Різниця:** Тільки знак порівняння (`<` замість `>`)

---

##### 3️⃣ **Алгоритм сортування за індексами**

```javascript
// 1. Розділяємо елементи за парністю індексів
let evenIndexElements = [];  // індекси 0, 2, 4, 6...
let oddIndexElements = [];   // індекси 1, 3, 5, 7...

for (let i = 0; i < array.length; i++) {
    if (i % 2 === 0) {
        evenIndexElements.push(array[i]);
    } else {
        oddIndexElements.push(array[i]);
    }
}

// 2. Сортуємо окремо
bubbleSortDescending(evenIndexElements);  // За спаданням
bubbleSortAscending(oddIndexElements);    // За зростанням

// 3. Об'єднуємо назад
let evenIndex = 0;
let oddIndex = 0;
for (let i = 0; i < array.length; i++) {
    if (i % 2 === 0) {
        array[i] = evenIndexElements[evenIndex++];
    } else {
        array[i] = oddIndexElements[oddIndex++];
    }
}
```

**Логіка:**
- Елементи на парних позиціях (0, 2, 4...) сортуємо за спаданням
- Елементи на непарних позиціях (1, 3, 5...) сортуємо за зростанням
- Зберігаємо структуру парність/непарність індексів

---

## 📈 ЛАБОРАТОРНА РОБОТА №5 - Chart.js

### **Що таке Chart.js?**
Chart.js - це JavaScript бібліотека для створення інтерактивних графіків та діаграм на веб-сторінках.

### **Основні типи діаграм в вашій роботі:**

#### 1️⃣ **Radar Chart (Діаграма-павутинка)**

```javascript
const carsRadarChart = new Chart(ctx, {
    type: 'radar',  // Тип діаграми
    data: carsData,
    options: {
        responsive: true,
        maintainAspectRatio: false,
        // ... інші налаштування
    }
});
```

**Для чого використовується:**
- Порівняння кількох об'єктів за багатьма параметрами
- У вашому випадку: порівняння автомобілів (BMW vs Mercedes)

**Основні параметри:**
```javascript
datasets: [{
    label: 'BMW M3',
    data: [510, 290, 300, 12, 8, 2],  // Значення по осях
    backgroundColor: 'rgba(54, 162, 235, 0.3)',  // Колір заливки
    borderColor: 'rgb(54, 162, 235)',  // Колір лінії
    borderWidth: 2
}]
```

**Що показує:**
- Потужність двигуна: 510 к.с.
- Макс. швидкість: 290 км/год
- Ресурс двигуна: 300 тис. км
- Кількість кольорів: 12
- Варіанти салону: 8
- Форми кузова: 2

---

#### 2️⃣ **Bubble Chart (Бульбашкова діаграма)**

```javascript
const youtubeChart = new Chart(ctx, {
    type: 'bubble',
    data: {
        datasets: youtubeChannels.map(channel => ({
            label: channel.name,
            data: [{
                x: channel.duration,      // Горизонтальна вісь
                y: channel.views,         // Вертикальна вісь
                r: channel.subscribers * 3 // Розмір бульбашки
            }],
            backgroundColor: getCategoryColor(channel.category)
        }))
    }
});
```

**Для чого використовується:**
- Відображення 3 параметрів одночасно (X, Y, розмір)
- У вашому випадку: аналіз YouTube каналів

**Три виміри даних:**
- **X (duration):** Середня тривалість відео
- **Y (views):** Кількість переглядів
- **R (subscribers):** Кількість підписників (розмір кружка)

**Функція кольорів за категоріями:**
```javascript
function getCategoryColor(category) {
    const colors = {
        'гумор': 'rgba(75, 192, 75, 0.7)',
        'техно': 'rgba(54, 162, 235, 0.7)',
        'геймінг': 'rgba(153, 102, 255, 0.7)',
        'політика': 'rgba(255, 159, 64, 0.7)'
    };
    return colors[category] || 'rgba(201, 203, 207, 0.7)';
}
```

**Призначення:** Візуальне групування каналів за тематикою

---

#### 3️⃣ **Налаштування tooltip (підказок)**

```javascript
tooltip: {
    callbacks: {
        label: function(context) {
            const channel = youtubeChannels[context.datasetIndex];
            return [
                `Канал: ${channel.name}`,
                `Тривалість відео: ${channel.duration} хв`,
                `Перегляди: ${(channel.views / 1000000).toFixed(1)} млн`,
                `Підписники: ${channel.subscribers} млн`,
                `Тематика: ${channel.category}`
            ];
        }
    }
}
```

**Що робить:** При наведенні на бульбашку показує детальну інформацію про канал

---

### **Основні концепції Chart.js:**

#### ✅ **Canvas API**
```javascript
const ctx = document.getElementById('carsRadarChart').getContext('2d');
```
- Chart.js використовує HTML5 `<canvas>` для малювання
- `getContext('2d')` дає доступ до 2D графіки

#### ✅ **Responsive дизайн**
```javascript
responsive: true,
maintainAspectRatio: false
```
- Діаграма адаптується під розмір екрану
- Зручно на мобільних пристроях

#### ✅ **Підключення бібліотеки**
```html
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
```
- Використовуємо CDN (Content Delivery Network)
- Не потрібно завантажувати файли локально

---

## 🔧 Загальні концепції JavaScript в ваших роботах

### 1️⃣ **DOM маніпуляції**
```javascript
document.getElementById('elementId')  // Знаходимо елемент за ID
element.innerHTML = 'text'           // Змінюємо HTML вміст
element.innerText = 'text'           // Змінюємо текстовий вміст
element.value                        // Отримуємо значення input
```

**Різниця innerHTML vs innerText:**
- `innerHTML` - може містити HTML теги: `<b>текст</b>`
- `innerText` - тільки текст, теги відображаються як текст

### 2️⃣ **Обробка подій**
```javascript
<button onclick="functionName()">  // Клік на кнопку
window.onload = function() { }     // Виконується при завантаженні сторінки
```

**Альтернативний спосіб (більш сучасний):**
```javascript
document.getElementById('btn').addEventListener('click', functionName);
```

### 3️⃣ **Валідація даних**
```javascript
if (isNaN(num) || num <= 0) {
    alert('Будь ласка, введіть коректну кількість');
    return;
}
```

**Перевірки:**
- `isNaN()` - чи не є числом
- `num <= 0` - чи не від'ємне/нуль
- `num < 100000 || num > 999999` - чи в діапазоні
- `return` - припиняє виконання функції

### 4️⃣ **Копіювання масивів**
```javascript
// Щоб не змінювати оригінал:
let array = [];
for (let i = 0; i < originalArray.length; i++) {
    array[i] = originalArray[i];
}
```

**Сучасний спосіб:**
```javascript
let array = [...originalArray];  // Spread operator
// або
let array = JSON.parse(JSON.stringify(originalArray));  // Глибоке копіювання
```

### 5️⃣ **Математичні функції**
```javascript
Math.sqrt(x)        // Квадратний корінь
Math.abs(x)         // Модуль (абсолютне значення)
Math.floor(x)       // Округлення вниз
Math.ceil(x)        // Округлення вгору
Math.round(x)       // Округлення до найближчого
Math.random()       // Випадкове число 0-1
Math.pow(x, y)      // x в степені y (або x ** y)
x.toFixed(2)        // Округлення до 2 знаків після коми
```

### 6️⃣ **Типи даних та перетворення**
```javascript
parseFloat("3.14")  // Рядок → число з комою: 3.14
parseInt("42")      // Рядок → ціле число: 42
String(123)         // Число → рядок: "123"
Number("456")       // Рядок → число: 456
Boolean(1)          // Будь-що → true/false
```

**Хибні (falsy) значення в JavaScript:**
- `false`, `0`, `""` (порожній рядок), `null`, `undefined`, `NaN`

**Істинні (truthy) - все інше:**
- `true`, `1`, `"текст"`, `[]`, `{}`

### 7️⃣ **Логічні оператори**
```javascript
&&  // І (AND) - обидві умови true
||  // АБО (OR) - хоча б одна true
!   // НЕ (NOT) - інверсія
==  // Дорівнює (з приведенням типів)
=== // Строго дорівнює (без приведення)
!=  // Не дорівнює
!== // Строго не дорівнює
```

**Приклади:**
```javascript
"5" == 5    // true (рядок приводиться до числа)
"5" === 5   // false (різні типи)
true && true   // true
true && false  // false
true || false  // true
!true          // false
```

### 8️⃣ **Цикли**
```javascript
// For цикл
for (let i = 0; i < n; i++) {
    // код
}

// While цикл
while (умова) {
    // код
}

// Do-while цикл
do {
    // код
} while (умова);
```

### 9️⃣ **Умовні оператори**
```javascript
// If-else
if (умова) {
    // код
} else if (інша_умова) {
    // код
} else {
    // код
}

// Тернарний оператор
let result = (умова) ? значення_якщо_true : значення_якщо_false;

// Switch
switch (значення) {
    case 1:
        // код
        break;
    case 2:
        // код
        break;
    default:
        // код
}
```

---

## 💡 Поради для захисту роботи

### **Що може запитати викладач:**

#### ❓ "Яка складність алгоритму Heap Sort?"
**Відповідь:** O(n log n) у всіх випадках (найкращий, середній, найгірший). Це робить його ефективнішим за Bubble Sort O(n²).

#### ❓ "Чому Bubble Sort має оптимізацію через swapped?"
**Відповідь:** Якщо за повний прохід не було жодного обміну, масив вже відсортований. Це дозволяє завершити роботу раніше та економить час.

#### ❓ "Як працює heapify?"
**Відповідь:** Heapify підтримує властивість max-heap: батьківський вузол завжди більший або рівний дочірнім. Працює рекурсивно зверху вниз, порівнюючи батька з дітьми.

#### ❓ "Чому використовуєте Chart.js, а не малюєте вручну?"
**Відповідь:** Chart.js спрощує створення складних діаграм, забезпечує інтерактивність, адаптивність та кросбраузерну сумісність. Також підтримує різні типи графіків з мінімальним кодом.

#### ❓ "Що таке Canvas?"
**Відповідь:** HTML5 елемент для малювання графіки через JavaScript. Використовується для динамічної візуалізації - Chart.js малює графіки саме на Canvas.

#### ❓ "Як обчислюєте площу трикутника?"
**Відповідь:** Використовую формулу через координати вершин (векторний добуток): `S = |x1(y2-y3) + x2(y3-y1) + x3(y1-y2)| / 2`. Це ефективніше формули Герона для координат.

#### ❓ "Чому використовуєте parseFloat, а не parseInt?"
**Відповідь:** `parseFloat` обробляє дробові числа (3.14), а `parseInt` тільки цілі. Для координат та геометричних обчислень потрібна точність з комою.

#### ❓ "Що таке модуль числа (%)?"
**Відповідь:** Оператор `%` повертає остачу від ділення. Наприклад: `10 % 3 = 1`, `15 % 5 = 0`. Використовується для перевірки кратності чисел.

#### ❓ "Чому сортуєте двомірний масив через одномірний?"
**Відповідь:** Heap Sort працює з одномірними масивами. Тому розгортаю 2D→1D, сортую, потім згортаю назад. Це зберігає логіку алгоритму без модифікації.

#### ❓ "Що таке рекурсія?"
**Відповідь:** Функція, яка викликає саму себе. У `heapify` використовується для обходу дерева вниз - після обміну елементів викликаємо heapify для піддерева.

#### ❓ "Для чого потрібна валідація даних?"
**Відповідь:** Захист від некоректного вводу користувача: порожні поля, не числа, вихід за межі діапазону. Без валідації програма може видати помилку або некоректний результат.

#### ❓ "Що таке event handling (обробка подій)?"
**Відповідь:** Реакція програми на дії користувача (клік, введення тексту). Використовую `onclick` для кнопок - при натисканні викликається відповідна функція.

#### ❓ "Різниця між == та ===?"
**Відповідь:** 
- `==` порівнює значення з приведенням типів (`"5" == 5` → true)
- `===` строге порівняння без приведення (`"5" === 5` → false)
- Рекомендується використовувати `===` для точності.

---

## 🎓 Ключові терміни

| Термін | Пояснення |
|--------|-----------|
| **Heap (Купа)** | Спеціальна структура даних - дерево з властивістю: батько ≥ дітей (max-heap) |
| **Heapify** | Процес перетворення піддерева в купу - порівняння батька з дітьми |
| **Bubble Sort** | Алгоритм сортування порівнянням сусідніх елементів |
| **Time Complexity** | Складність алгоритму - як час виконання залежить від розміру даних |
| **Canvas** | HTML5 елемент для малювання графіки через JavaScript API |
| **CDN** | Content Delivery Network - мережа для швидкого завантаження бібліотек |
| **Responsive** | Дизайн що адаптується під різні розміри екранів |
| **DOM** | Document Object Model - представлення HTML в JavaScript |
| **Parsing** | Перетворення рядка в інший тип даних (parseFloat, parseInt) |
| **Validation** | Перевірка коректності введених даних |
| **Template Literals** | Шаблонні рядки з інтерполяцією змінних: `` `текст ${змінна}` `` |
| **Ternary Operator** | Скорочений if-else: `умова ? true_значення : false_значення` |
| **Event Handler** | Функція що викликається при певній події (клік, введення) |
| **Falsy/Truthy** | Значення що інтерпретуються як false/true в логічних операціях |
| **Recursion** | Функція що викликає саму себе |

---

## 📚 Структура всіх лабораторних робіт

### **LB1 - Перша веб-сторінка**
- Базовий HTML
- Підключення CSS та JS
- Виведення в консоль

### **LB2 - Обчислення та логіка**
- Геометричні обчислення (трикутник, парабола)
- Логічні перевірки (кратність, нерівності)
- Робота з датами
- Математичні функції
- Валідація вводу

### **LB3 - Масиви**
- Генерація випадкових масивів (1D, 2D)
- Видалення дублікатів
- Пошук мінімуму/максимуму
- Фільтрація елементів
- Добуток позитивних елементів
- Створення таблиць з масивів

### **LB4 - Сортування**
- **Завдання 1:** Heap Sort для двомірних масивів
  - Розгортання 2D→1D
  - Побудова heap
  - Функція heapify
  - Сортування за спаданням
- **Завдання 2:** Bubble Sort за індексами
  - Розділення за парністю індексів
  - Різні напрямки сортування
  - Об'єднання результатів

### **LB5 - Візуалізація (Chart.js)**
- **Radar Chart:** Порівняння автомобілів за параметрами
- **Bubble Chart:** Аналіз YouTube каналів
- Налаштування графіків
- Інтерактивні підказки (tooltips)
- Responsive дизайн
- Кольорове кодування категорій

---

## ✅ Чеклист перед захистом

### **Загальне розуміння:**
- [ ] Розумію різницю між HTML, CSS та JavaScript
- [ ] Знаю як підключати зовнішні файли
- [ ] Можу пояснити DOM та event handling
- [ ] Розумію валідацію даних

### **LB2 - Математика:**
- [ ] Можу пояснити формулу відстані між точками
- [ ] Знаю як знайти вершину параболи
- [ ] Розумію оператор модуля (%)
- [ ] Знаю різницю між parseFloat та parseInt

### **LB3 - Масиви:**
- [ ] Розумію як працює Math.random()
- [ ] Можу пояснити різницю між 1D та 2D масивами
- [ ] Знаю як обходити двомірний масив
- [ ] Розумію filter та інші методи масивів

### **LB4 - Алгоритми:**
- [ ] Знаю кроки алгоритму Heap Sort
- [ ] Розумію функцію heapify та індекси в бінарному дереві
- [ ] Можу описати Bubble Sort
- [ ] Знаю складність O(n), O(n log n), O(n²)
- [ ] Розумію чому heapify - рекурсивна функція

### **LB5 - Графіки:**
- [ ] Розумію призначення radar та bubble charts
- [ ] Можу пояснити структуру даних в Chart.js
- [ ] Знаю що таке Canvas API
- [ ] Розумію як працюють tooltips

### **JavaScript концепції:**
- [ ] Знаю різницю між == та ===
- [ ] Розумію тернарний оператор
- [ ] Можу пояснити switch statement
- [ ] Знаю що таке template literals
- [ ] Розумію логічні оператори (&&, ||, !)

---

## 🚀 Успіхів на захисті!

**Головне:** Не бійтеся сказати "не пам'ятаю, але можу глянути в коді". Важливо розуміти логіку, а не пам'ятати кожен рядок напам'ять.
